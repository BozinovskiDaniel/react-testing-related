import ExpandableText from "../components/ExpandableText";
import Onboarding from "../components/Onboarding";
import TermsAndConditions from "../components/TermsAndConditions";

const PlaygroundPage = () => {
    return (
        <ExpandableText text="Long text that exceeds the limit Long text that exceeds the limit Long text that exceeds the limit Long text that exceeds the limit Long text that exceeds the limit Long text that exceeds the limit Long text that exceeds the limit Long text that exceeds the limit" />
    );
};

export default PlaygroundPage;
